#include "gpio.h"

bool btn_state;

int main(void)
{
	 /* Initialize LED */
	 led_init();
	 /* Initialize Pushbutton */
	 btn_init();

	 while(1)
	 {
		 /* Get Pushbutton State */
		 btn_state = btn_get_state();

		 if(btn_state)
		 {
			 led_on();
		 }
		 else
		 {
			 led_off();
		 }
	 }
}
